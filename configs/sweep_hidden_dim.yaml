# Hydra multirun sweep for hidden_dim.
# Usage:
#   python -m lstm_scaling.experiments.scaling_hidden_dim -m --config-name sweep_hidden_dim

defaults:
  - base
  - _self_

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      hidden_dim: 128,160,192,256,320,384,512,640,768,1024,1280,1536,2048
